{
  "name": "playwright_api_and_web",
  "version": "1.0.0",
  "description": "",
  "author": "Zhivko Kostadinov",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^8.3.1",
    "@playwright/test": "^1.40.1",
    "@types/node": "^20.10.5"
  },
  "scripts": {
    "tests:api": "npx playwright test --config=playwright.config.js --workers=1 --retries=2 --grep @api",
    "tests:web": "npx playwright test --config=playwright.config.js --workers=1 --retries=2 --grep @web",
    "tests:all": "npx playwright test --ui"
  }
}
